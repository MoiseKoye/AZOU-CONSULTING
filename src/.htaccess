<IfModule mod_rewrite.c>
  RewriteEngine On
  # If the requested file or directory doesn't exist, rewrite to index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
